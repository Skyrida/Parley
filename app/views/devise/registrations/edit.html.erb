<div class="profileheader">

<% if user_signed_in? && current_user.photo.attached? %>
            <%= cl_image_tag current_user.photo.key, crop: :fill, class: "profile-icon"%>
          <% else %>
            <%= image_tag "profile-icon.png", class: "profile-icon" %>
          <% end %>
</div>


<div class="username" >
  <%= current_user.username %>

</div>

<div class="main-container-block">
  <div class="profilecard">

  <div class="d-flex">
    <p class="leftside"><strong><em>First name:</em></strong></p>
    <p class="rightside"><%= "#{current_user.first_name}" %></p>
  </div>


  <div class="d-flex">
    <p class="leftside"><strong><em>Last name:</em></strong></p>
    <p class="rightside"><%= "#{current_user.last_name}" %></p>
  </div>
  <div class="d-flex">
    <p class="leftside"><strong><em>Location:</em></strong></p>
    <p class="rightside"><%= "#{current_user.location}" %></p>
  </div>


  <div class="d-flex">
    <p class="leftside"><strong><em>Email:</em></strong></p>
    <p class="rightside"><%= "#{current_user.email}" %></p>
  </div>


  <div class="d-flex">
    <div class="user-interessts">
    <p class="leftside"><strong><em>Interests:</em></strong></p>
    <div class="intrest-tags rightside">
                <% current_user.tags.each do |tag_name| %>
                      <div class="blue-tag">
                        <p><%= tag_name  %></p>
                      </div>
                    <% end %>
    </div>
    </div>
    </div>

    <div class="makeitcentered">
    <button class="profile-button" type="submit" style="width: 120px;">Edit Profile</button>
    <%= button_to "Logout", destroy_user_session_path, method: :delete, class: "profile-button", style:"width: 120px;" %>
  </div>
  </div>
</div>


<div data-controller="userdebatesearch" class="main-container-block">
  <div class="profilesearch" style="width:100%">
    <div  data-userdebatesearch-target="searchbarform" style="width:100%">
      <%= form_with url: debates_path, data: { action: "keyup->userdebatesearch#searchFunction", userdebatesearch_target: "formQuery" } do |f| %>
        <%= f.text_field  :query, class: 'searchquery' , placeholder:"search your things..."  %>
      <% end %>
    </div>
  </div>
<div class= "main-container-block"></div>
<div class="firsth">
  <H1><strong>Your debates</strong></H1>
</div>
  <div data-userdebatesearch-target="debatesCards">
    <div class="yourdebates">
      <% current_user.your_debates.each do |debate| %>
          <% if debate.arguments.count > 0 %>
            <%= link_to debate_arguments_path(debate.id), class: "text" do %>
              <div class="debate-card">
                <div>
                  <div class="header-debate-card">
                    <div class="tag-names">
                      <% debate.tag_list.each do |tag_name| %>
                        <div class="blue-tag">
                          <p><%= tag_name  %></p>
                        </div>
                      <% end %>
                    </div>
                    <div class="debate-header-picture">
                      <%= debate.arguments.count %>
                      <%= image_tag "blue-comment.png" %>
                    </div>
                  </div>
                  <h2 class="debate-card-title"><b><%= debate.title %></b></h2>
                  <p class="debate-card-text"><%=  debate.description %></p>
                </div>
              </div>
            <% end %>
          <% else %>
              <%= link_to debate_path(debate.id), class: "text" do %>
                <div class="debate-card">
                  <div>
                    <div class="header-debate-card">
                      <div class="tag-names">
                        <% debate.tag_list.each do |tag_name| %>
                          <div class="blue-tag">
                            <p><%= tag_name  %></p>
                          </div>
                        <% end %>
                      </div>
                      <div class="debate-header-picture">
                        <%= debate.arguments.count %>
                        <%= image_tag "blue-comment.png" %>
                      </div>
                    </div>
                    <h2 class="debate-card-title"><b><%= debate.title %></b></h2>
                    <p class="debate-card-text"><%=  debate.description %></p>
                  </div>
                </div>
              <% end %>
          <% end %>
        <% end %>
    </div>

    <div class="firsth">
      <H1><strong>Your arguments</strong></H1>
    </div>

    <div class="yourdebates">
      <% current_user.your_arguemtens.each do |debate| %>
          <% if debate.arguments.count > 0 %>
            <%= link_to debate_arguments_path(debate.id), class: "text" do %>
              <div class="debate-card">
                <div>
                  <div class="header-debate-card">
                    <div class="tag-names">
                      <% debate.tag_list.each do |tag_name| %>
                        <div class="blue-tag">
                          <p><%= tag_name  %></p>
                        </div>
                      <% end %>
                    </div>
                    <div class="debate-header-picture">
                      <%= debate.arguments.count %>
                      <%= image_tag "blue-comment.png" %>
                    </div>
                  </div>
                  <h2 class="debate-card-title"><b><%= debate.title %></b></h2>
                  <p class="debate-card-text"><%=  debate.description %></p>
                </div>
              </div>
            <% end %>
          <% else %>
              <%= link_to debate_path(debate.id), class: "text" do %>
                <div class="debate-card">
                  <div>
                    <div class="header-debate-card">
                      <div class="tag-names">
                        <% debate.tag_list.each do |tag_name| %>
                          <div class="blue-tag">
                            <p><%= tag_name  %></p>
                          </div>
                        <% end %>
                      </div>
                      <div class="debate-header-picture">
                        <%= debate.arguments.count %>
                        <%= image_tag "blue-comment.png" %>
                      </div>
                    </div>
                    <h2 class="debate-card-title"><b><%= debate.title %></b></h2>
                    <p class="debate-card-text"><%=  debate.description %></p>
                  </div>
                </div>
              <% end %>
          <% end %>
        <% end %>
    </div>

    <div class="firsth">
      <H1><strong>Your likes</strong></H1>
    </div>

    <div class="yourdebates">
      <% current_user.your_likes.each do |debate| %>
          <% if debate.arguments.count > 0 %>
            <%= link_to debate_arguments_path(debate.id), class: "text" do %>
              <div class="debate-card">
                <div>
                  <div class="header-debate-card">
                    <div class="tag-names">
                      <% debate.tag_list.each do |tag_name| %>
                        <div class="blue-tag">
                          <p><%= tag_name  %></p>
                        </div>
                      <% end %>
                    </div>
                    <div class="debate-header-picture">
                      <%= debate.arguments.count %>
                      <%= image_tag "blue-comment.png" %>
                    </div>
                  </div>
                  <h2 class="debate-card-title"><b><%= debate.title %></b></h2>
                  <p class="debate-card-text"><%=  debate.description %></p>
                </div>
              </div>
            <% end %>
          <% else %>
              <%= link_to debate_path(debate.id), class: "text" do %>
                <div class="debate-card">
                  <div>
                    <div class="header-debate-card">
                      <div class="tag-names">
                        <% debate.tag_list.each do |tag_name| %>
                          <div class="blue-tag">
                            <p><%= tag_name  %></p>
                          </div>
                        <% end %>
                      </div>
                      <div class="debate-header-picture">
                        <%= debate.arguments.count %>
                        <%= image_tag "blue-comment.png" %>
                      </div>
                    </div>
                    <h2 class="debate-card-title"><b><%= debate.title %></b></h2>
                    <p class="debate-card-text"><%=  debate.description %></p>
                  </div>
                </div>
              <% end %>
          <% end %>
        <% end %>
    </div>
  </div>
  </div>
